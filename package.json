{
  "name": "tutti",
  "version": "0.0.0",
  "private": true,
  "description": "Tutti — a deterministic cooking-scheduling engine that turns a set of recipes into one parallel, self-adjusting cooking plan.",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspace apps/web",
    "dev": "npm run dev --workspace apps/web",
    "smoke": "node scripts/smoke.mjs",
    "pwa-check": "node scripts/pwa-check.mjs",
    "perf-check": "node scripts/perf-check.mjs",
    "gate": "npm run typecheck && npm run test && npm run build && npm run pwa-check && npm run perf-check && npm run smoke"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
