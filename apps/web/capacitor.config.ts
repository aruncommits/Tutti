// Stub for the future native (iOS/Android) wrap (Capacitor). Not wired up yet —
// the responsive web app is the present target; this marks the intended path.
// Kept dependency-free (no @capacitor/cli yet) so it typechecks in the loop.
const config = {
  appId: "app.tutti",
  appName: "Tutti",
  webDir: "dist",
};

export default config;
